export default function TabReminders() {
  return (
    <div className="tab-pane">
      <h3 className="section-title">提醒（Phase 3）</h3>
      <p className="muted">
        当前选中节点的提醒列表将在此显示。
      </p>
      <p className="muted">
        MVP 阶段先做基础节点编辑和文件操作，提醒系统在后续 Phase 实现。
      </p>
    </div>
  );
}
